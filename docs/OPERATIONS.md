# ??? AI ?????? - ??????????? (OPERATIONS.md)

> **?????**?1C vCPU / 896MB RAM / 2GB Swap / 30GB ??  
> **??????**?root  
> **???????**?`/root/abc-project`  

---

## 1. ???????????

| ???? | ???? | ????/?? | ????? |
| :--- | :--- | :--- | :--- |
| **Nginx** | systemd (`nginx.service`) | `0.0.0.0:80`, `0.0.0.0:443` | ???????????????????? |
| **Quant Backend** | systemd (`quant.service`) | `127.0.0.1:8000` | FastAPI ???????????????? |
| **PostgreSQL 16** | Docker (`enterprise-ai-quant-postgres`) | `127.0.0.1:5432` | ?????????????????? |
| **Redis 7** | Docker (`enterprise-ai-quant-redis`) | `127.0.0.1:6379` | ?????????????????AOF??? |
| **fail2ban** | systemd (`fail2ban.service`) | ????? `/var/log/secure` | SSH ??????? |

---

## 2. ?????????

### 2.1 ???????
```bash
# ??????
systemctl status quant

# ????????? .env?
systemctl restart quant

# ??????
journalctl -u quant -f -n 50

# ???????????
/root/abc-project/deploy/deploy.sh
```

### 2.2 ????????
```bash
# ????????
docker ps -a

# ?????? Redis ??
docker restart enterprise-ai-quant-postgres enterprise-ai-quant-redis

# ?????????
/root/abc-project/deploy/backup.sh
```

### 2.3 ??????
```bash
# ?? fail2ban ????
fail2ban-client status sshd

# ?????? IP
fail2ban-client set sshd unbanip <IP_ADDRESS>
```

---

## 3. ???????????

### 3.1 ??????
* ????????? `03:00` ???? `/root/abc-project/deploy/backup.sh`
* ?????`/root/abc-project/backups/quant_backup_YYYYMMDD_HHMMSS.sql.gz`
* ????????? 7 ?????????????

### 3.2 ????????
```bash
# 1. ??????
systemctl stop quant

# 2. ????????????
gunzip -c /root/abc-project/backups/quant_backup_YYYYMMDD_HHMMSS.sql.gz | docker exec -i enterprise-ai-quant-postgres psql -U quant -d quant

# 3. ???????
systemctl start quant
curl -s http://127.0.0.1:8000/health
```

---

## 4. 896MB ??????????? OOM ??

1. **Worker ????**??? `quant.service` ????? Worker?`--workers 1`????????????????? OOM?
2. **????????**?????????? Prometheus Server?Grafana ? ELK ???????????? API `/metrics` ? `/health` ???
3. **??????**?
   ```bash
   # ????? Swap ?????? available ?? >= 300MB?
   free -h
   # ?????????????? >= 5GB?
   df -h /
   ```
